实施状态：**1/25 项完成（4.4 / F15，独立修复分支已通过本地回归）**。上游 PR 尚未创建；本规划分支不包含业务补丁。Fxx 对应 `audit/2026-09-08/AUDIT.md`，T/U 为 BUG 复现编号；实施时另建正确行为回归，不覆盖历史证据。每组修改前执行 impact，提交前执行 detect_changes；Perl UNKNOWN 用源码与对应回归补足。按 [上游 PR 流程与拆分](upstream-pr-workflow.md) 在独立修复分支提交，五阶段只作任务分类，不要求按整阶段形成大 PR。跨 PR 任务在全部相关实现与验证完成前保持未勾选；上游合并状态单独记录。

用户确认后置 F09/F10/F25，三项不属于本 change 必做范围，不能勾选或报告为已修复。下面任务只覆盖本次必要修复与已有行为兼容；其他破坏性扩张需另行确认。

## 1. 请求错误与数据合同

- [ ] 1.1 修正请求失败/结果未知状态及 zfs/iscsi 包装器判断（F01）；用 timeout、EOF、API error 与合法成功代表例证明失败不再返回成功且不会盲重放写请求。
- [ ] 1.2 归一化 JSON-RPC/DDP 解析与错误，移除 null 文本替换（F20–21）；验证普通 code/message、非法 JSON、合法 null/false/0 和嵌套 null，无二次异常。
- [ ] 1.3 移除泛化数字字符串转换及输入对象改写（F22）；验证前导零认证、已知数字字段、嵌套输入保持原值。
- [ ] 1.4 修正版本解析输入（F28）；验证 TrueNAS、TrueNAS-Scale 与非法版本三例，并运行本 PR 相关分组测试后提交。

## 2. 有界连接、协议和凭据

- [ ] 2.1 实现连接/握手/写入/响应的有限期限、心跳失败重连与实际重试预算（F13、F16）；静默握手、心跳 timeout 和预算耗尽均能有界结束，已发送写请求不重复发送。
- [ ] 2.2 修正 masked frame、短写、缓冲消费、Ping/Pong/空 Close 与断开状态清理（F17–18）；通过 wire 位、短写替身、合并帧和控制帧分组回归。
- [ ] 2.3 实现 JSON-RPC/DDP 当前响应 ID 关联（F19）；验证通知/异号先到不会完成请求，匹配响应正常返回且等待期限不重置。
- [ ] 2.4 删除或脱敏配置、认证和原始收发日志，统一 warn 级别别名（F11）；debug 假凭据测试确认 key/password 不出现在日志，错误仍可诊断。
- [ ] 2.5 修正 host-only 缓存、失败初始化缓存和错误字段检查（F12），加入最小 PID/连接状态隔离；验证同 host 凭据轮换、初始化失败重试和子进程独立连接，以及旧配置仍原位读取、SSL=0 实际 TLS 与验证策略保持不变后，运行本 PR 相关分组测试并提交。

## 3. PVE 存储生命周期

- [ ] 3.1 校验 target/extent/mapping/lunid 和枚举结果，统一实际上限但不缩小现有有效 ID 范围（F03、F07）；验证缺失/非法 ID 拒绝，LUN 0 有效，枚举失败不分配 0。
- [ ] 3.2 核实并复用 PVE 现有同 target 分配协调，覆盖本集群跨 storage ID 入口，再实现有限冲突处理与创建补偿（F05、F07）；代表性竞争测试须覆盖查询与写入交错，不能仅注入服务端冲突；明确中间失败与未知响应不盲重放、不清理预存对象。若现有机制不足则报告阻断，不引入新锁服务。
- [ ] 3.3 修正删除/重建的逐步检查和首错保留（F04）；验证首步删除失败不继续破坏性操作，Patch 返回 PVE 可见失败。
- [ ] 3.4 让 Native 变更回调和大小查询传播真实结果（F02、F24）；分组覆盖 clone/resize/free/template/rename/snapshot 失败，并确认有效成功按 PVE 合同返回。
- [ ] 3.5 优先原位保持 rename/template/resize 的 dataset、extent、serial/NAA 和 LUN 身份（F06）；验证不会自动 force 删除/重建在用 extent，无法无损完成时保留对象并明确报错，中间失败不报告成功。
- [ ] 3.6 统一新卷 parent/name、snapshot 短名和实际 dataset 转换，并兼容历史不带 parent 的合法卷名（F23）；验证两种卷名快照路径一致、不改写现有 VM 配置，检查排序阻挡及相似 pool 前缀，已有父类过滤不重复实现。
- [ ] 3.7 只拒绝 Native 危险的本机 ZFS stream 继承入口（F08），限定其他继承能力和未实现 Patch 分派；固定父类命令拦截验证无本机 zfs 写副作用，确认同一共享存储节点迁移路径未被一并禁用，记录 stream 迁移受限，并核对 PVE 9 扩展回调参数不会误操作当前卷。运行本 PR 相关回归后提交。

## 4. 安装、生成脚本与界面

- [ ] 4.1 修正部署参数、依赖、模式、版本和补丁资源预检（F26–27）；未知参数、缺失版本和资源在系统修改前立即非零，8/9 正常资源可进入后续流程。
- [ ] 4.2 修正首次安装/恢复与关键命令失败即停语义，收敛成功重启服务范围（F14）；命令替身验证失败 cp/patch 不重启，首次无旧备份可正常处理，成功不重启 corosync/pve-cluster。
- [ ] 4.3 修正 build 路径/输入/错误处理和临时产物替换（F27）；验证 diff 正常差异成功、缺失输入失败且旧补丁不变。
- [x] 4.4 同步修正 PVE 8/9 API key field reference（F15）；两版切入/切出 TrueNAS provider 不抛异常，不适用字段清理符合表单预期。
- [ ] 4.5 更新 README 的 Native/Patch、版本基线、既有 TLS/凭据限制和后置触发条件、debug 行为及 stream 迁移限制，将 API viewer/旧 orig 补丁明确标为历史参考而非支持安装产物（F27）；检查文档与安装路径一致，运行本 PR 相关回归后提交。

## 5. 集成、证据与文档同步

- [ ] 5.1 汇总执行前四阶段分组回归及必要语法/补丁校验，对 F01–F28 分别记录本次修复或用户同意后置（F09/F10/F25），复核四项 capability 覆盖；输出一次摘要，明确真实 PVE/TrueNAS/iSCSI 尚未验证，不重复穷举同质对象。
- [ ] 5.2 独立复核主要数据不变量、失败副作用和规格实现一致性，解决实际发现；记录所用固定父类/协议依赖版本及剩余真实环境风险，验证没有把历史 BUG 断言通过当作修复完成。
- [ ] 5.3 释放本任务使用的 wsx 容器/目录及本地临时产物，保留共享服务；通过资源清理结果确认完成。未使用远程资源时记录不适用。
- [ ] 5.4 如实同步审计修复状态、README 与本 tasks，逐 PR 同步必要文档，执行 OpenSpec 校验和 GitNexus 变更分析后提交规划状态更新，分别记录 PR 链接与合并状态；只有实际实现与验证证明完成才勾选，不在本任务自动部署、发布或合并。

真实环境联调为后置风险承接：准备部署到真实 PVE/TrueNAS 时再明确环境和授权，不展开为本 change 的强制资格任务。
